# Save your photos here

Drop the six photos into this folder using these exact filenames so the one-pager picks them up automatically. Square crops work best (around 400×400px or larger).

| Filename | Who |
|----------|-----|
| `lilli.jpg` | Lilli Graf (for the byline at the top of the page) |
| `ariel.jpg` | Ariel Chamberlain (Octopus Pond) |
| `tiana.jpg` | Tiana Veldwisch |
| `rouse.jpg` | Sara Frisk + Aradhana Goel (the photo with both of them) |
| `christine.jpg` | Christine Maggi (Design Gal) |
| `brendan.jpg` | Brendan Rodgers (Thread & Stack) |

`.jpg`, `.jpeg`, `.png`, or `.webp` all work — just keep the filename (without extension) as listed above and update the extension in the HTML if you use something other than `.jpg`. The HTML falls back to initials if a file is missing, so nothing breaks if a photo isn't ready yet.
